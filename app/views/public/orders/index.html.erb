<h4>注文履歴一覧</h4>

<table class="table">
  <tr>
    <th>注文日</th>
    <th>配送先</th>
    <th>注文商品</th>
    <th>支払金額</th>
    <th>ステータス</th>
    <th>注文詳細</th>
  </tr>
  <% @orders.each do |order|%>
  <tr>
    <td><%#= order_detail.created_at %></td>
    <td><%= order.shipping_address%></td>
    <td><%= order.product.name %></td>
    <td><%= order.total_bill_amount + order.shipping_fee %></td>
    <td><%= order.order_status %></td>
    <td></td>
  </tr>
  <% end %>

</table>></td>
  </tr>
  <% end %>

</table>